upstream apicluster {
    {% for srv in groups.allezon %}
    server {{ srv }}:8082;
    {% endfor  %}
}

server {
    listen 6666 default_server;
    listen [::]:6666 default_server;
    server_name _;

    location / {
        proxy_pass http://apicluster;
    }
}
